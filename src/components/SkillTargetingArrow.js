import React, { Component, useState } from "react";
import { useSelector, useDispatch } from 'react-redux';
import { drawSkillInformationWindowAction } from '../actions';
import styles from "../css/SkillTargetingArrow.module.css"
import SkillInformationWindow from './Skill_InformationWindow';

export default function SkillTargetingArrow(props) {

  return (

    < div className={styles.Body}>
    </div >
  );

}